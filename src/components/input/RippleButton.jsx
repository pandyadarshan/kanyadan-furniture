import React, { useRef } from "react";
import { gsap } from "gsap";

const RippleButton = () => {
  const handleClick = (e) => {};

  return <button className="cta mt-0">Inquiry Now</button>;
};

export default RippleButton;
